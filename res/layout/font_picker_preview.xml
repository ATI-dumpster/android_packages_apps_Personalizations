<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/font_selector_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/font_selector_title"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/font_selector"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/font_selector_title"
        android:layout_marginTop="8dp"
        android:background="@drawable/rounded_bg"
        android:ellipsize="end"
        android:maxLines="1"
        android:padding="12dp"
        android:text="@string/font_picker_default"
        android:textSize="16sp" />

    <!-- Custom Font Button -->
    <Button
        android:id="@+id/custom_font_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/font_selector"
        android:layout_marginTop="16dp"
        android:text="Import Custom Font"
        android:textAllCaps="false" />

    <TextView
        android:id="@+id/font_preview_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/custom_font_button"
        android:layout_marginTop="24dp"
        android:text="@string/font_preview_title"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/font_preview_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/font_preview_title"
        android:layout_marginTop="8dp"
        android:background="@drawable/rounded_bg"
        android:padding="16dp"
        android:text="@string/font_preview_text"
        android:textSize="16sp" />

    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/apply_extended_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"
        android:text="@string/apply_text"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Button"
        app:icon="@drawable/ic_check_24dp" />

</RelativeLayout>
